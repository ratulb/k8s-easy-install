#!/usr/bin/env bash
# Docker is no longer used by this project.
# Kubernetes uses containerd directly as the container runtime.
# Containerd is installed and configured by install-kubeadm.sh.
echo "Docker installation is no longer needed. Kubernetes uses containerd directly."
echo "See install-kubeadm.sh for containerd configuration."
